<footer class="site-foot">
  <div class="site-foot__logo">
    <a href="/">
      <span style="font-family: var(--font-bigger); font-size: 3rem; font-weight: 700; color: var(--color-secondary);">UR</span>

      <span class="u-sr-only">Umesh Reddy Somula</span>
    </a>
  </div><!-- .site-foot__logo -->
</footer>

<style lang="scss">
  .site-foot {
    @include flex(row, center, center);

    position: relative;
    z-index: 3;

    padding: 1.5rem;

    border-top: 1px solid color(secondary);

    @include mq(phone) {
      padding: 1rem;
    }

    &__logo {
      svg {
        display: block;
        width: 3rem;
        height: 3rem;

        @include mq(phone) {
          width: 1.875rem;
          height: 1.875rem;
        }
      }
    }
  }
</style>
